<!DOCTYPE html>
<html lang="zh-Hant">
<head>
  <meta charset="UTF-8">
  <meta
    name="viewport"
    content="width=800, initial-scale=1, maximum-scale=1, user-scalable=no"
  >
  <title><%= title %></title>
  <link rel="stylesheet" href="/css/glass.css">
</head>

<body>
  <main class="dashboard">
    <section class="panel">
      <h1><%= title %></h1>
      <p class="build"><%= build %></p>

      <div class="date"><%= date %></div>
      <div class="weekday"><%= weekday %></div>

      <div class="status">Dashboard is ready</div>
    </section>
  </main>
</body>
</html>